<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Document</title>
		<script src="https://cdn.tailwindcss.com"></script>
		<link rel="stylesheet" href="/stylesheets/style.css" />
	</head>
	<body>
		<div class="w-full h-full bg-zinc-900 text-white px-6 py-6">
			<a class="px-3 py-3 bg-blue-500 rounded-lg cursor-pointe" href="/"
				>Back to Home</a
			>
			<h1 class="text-4xl mt-10 font-bold mb-6">ALL USERS</h1>
			<div class="users flex flex-wrap gap-6">
				<% if (users.length > 0) { %>
                    <% users.forEach(function(user){ %>
				<div
					class="user max-w-72 p-6 rounded-xl bg-zinc-800 shadow-lg hover:shadow-xl transition">
					<div
						class="w-32 h-32 rounded-full mx-auto bg-red-500 overflow-hidden flex items-center justify-center">
						<img class="w-full h-full object-cover" src="<%= user.image %>" />
					</div>
					<h3 class="text-xl font-semibold mt-3 tracking-tighter">
						<%= user.name %>
					</h3>
					<h5 class="text-zinc-500"><%= user.email %></h5>
					<div class="flex justify-center gap-3 mt-3">
						<a class="text-yellow-600" href="/edit/<%= user._id %>">Edit User</a>
						<a class="text-red-600" href="/delete/<%= user._id %>"
							>Delete User</a
						>
					</div>
				</div>
				<% }) %>
                    <% } else { %>
                        <h3>No Users Yet....</h3>
                        <% } %>
			</div>
		</div>
	</body>
</html>
